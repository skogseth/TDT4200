# TDT4200 - Problem Set 2 - Fall 2021
## Graded MPI

### Setup
1. Create the directories `build` and `build-serial`
2. Run `make main` or `make main-serial`
    - `main` is the MPI implementation. `main-serial` is a pure C implementation.
